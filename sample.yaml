sample:
  description: weather station app in Rust
  name: weather station app
common:
  harness: console
  harness_config:
    type: one_line
    regex:
    - "weather station app rust on (.*)"
    filter: CONFIG_RUST_SUPPORTED
tests:
  sample.rust.hellorust:
    tags: introduction
